NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Max API requests per 60s the client sends before queueing (keep below the
# backend THROTTLE_LIMIT, default 300, to leave room for other tabs). Optional.
# NEXT_PUBLIC_API_RATE_LIMIT=240
